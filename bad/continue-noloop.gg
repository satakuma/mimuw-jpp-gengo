fn f(int n) -> int {
    int ret = n * 2;
    continue; // error
    return ret;
}

fn main() -> int {
    int i = 0;
    while (i < 42) {
        print(f(42));
        i = i + 1;
    }
    return 0;
}
